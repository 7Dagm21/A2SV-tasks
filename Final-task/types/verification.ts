export interface VerificationData {
  sessionId: string
  timestamp: number
  userAgent: string
  ipAddress: string
}

export interface Language {
  code: string
  name: string
  nativeName: string
}
